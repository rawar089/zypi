[package]
name = "zypi"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = "0.35.0"
quick-xml = { version = "0.41.0", features = ["serialize"] }
serde = { version = "1", features = ["derive"] }
